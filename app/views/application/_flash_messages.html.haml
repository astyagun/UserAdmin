- flash.each do |type, message|
  %div{class: "alert alert-dismissable #{bootstrap_class_for_flash(type)} fade show"}
    %button.close{'data-dismiss' => 'alert', 'aria-label' => 'Close'}
      %span{'aria-hidden' => true} &times;
    = message
