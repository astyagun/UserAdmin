- flash.each do |type, message|
  .alert.alert-dismissable.fade.show{class: bootstrap_class_for_flash(type), role: 'alert'}
    %button.close.align-top{'data-dismiss' => 'alert', 'aria-label' => 'Close'}
      %span{'aria-hidden' => true} &times;
    = message
